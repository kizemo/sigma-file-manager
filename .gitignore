# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
coverage
*.local

/cypress/videos/
/cypress/screenshots/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

flatpak/.deb-input/
flatpak/.build/
flatpak/.repo/
flatpak/output/

# Release artifacts — local mirror of GitHub Releases; do NOT commit
# binaries to git. Use `gh release` (or pull from GitHub Releases page) for
# the canonical copy. Local copies live in `release/` for offline use.
release/
